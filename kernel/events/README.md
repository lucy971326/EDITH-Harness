# events

【它是什么】进程内同步事件登记处插件。

【提供能力】注册登记处服务 `events`。

【使用能力】无。

【填充插槽】自身不填；监听者通过 `Subscribe[T]` 登记回调。

## 代码主干

```text
Subscribe[T]
  → 按 Go 类型登记监听者

Publish[T]
  → 复制当前监听名单
  → 按注册顺序同步通知全部监听者
  → 汇总错误返回发布者
```

它不保存、不缓冲事件；有返回值的协作仍然直接调用服务。
