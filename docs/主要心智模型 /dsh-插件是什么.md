# dsh 插件是什么、能做什么 — 最终核对版

> 已对照源码与文档逐条核实。例子都是仓库里真实存在的。

---

## 插件长什么样

一个插件就是**两样东西**：一个名字 + 一个 `apply(ctx)` 函数。就这么简单。

仓库里的真实例子 `fs-observation-policy`（文件"先读后改"策略插件），**全部代码 130 行**，核心就这些：

```ts
export const name = 'fs-observation-policy'          // 名字
export function apply(ctx: Context): void {          // 挂载时执行一次
  ctx.on('fs/write-intent', (target, actor) => ...)  // 挂三个事件监听
  ctx.on('fs/edit-intent',  (target, actor) => ...)
  ctx.on('fs/observed',     (target, o, actor) => ...)
}
```

它在 `apply` 里做的每次注册（`ctx.on` / `ctx.effect` / `ctx.provide`）都自动带着"撤回函数"——插件卸载时，它挂的监听、造的服务、留的状态**全部自动消失**，不留垃圾。

插件怎么被装上？配置文件里一行：

```yaml
- id: session
  name: '@deepseek-ai/dsh-session'
```

每个插件 = 配置里的一行（id + 名字 + 可选 config）。装不装、怎么配、替换成谁，全由配置层叠决定。

---

## 插件能做什么：四个自由度，从小到大

### ① 不写代码：纯配置扩展/替换

写一个 patch 文件（YAML），按 id 替换掉任何一行、插入新行。

- 把某个插件的 config 整个换掉（比如换默认模型）；
- 把某个插件换成另一个包（比如把 bash-local 换成 bash-sandbox）；
- **连 agent 主循环都能换**——因为它也只是配置里的一行。

### ② 写插件，但只挂别人：监听 + 注册（不占服务位）

写一个 `apply(ctx)`，只做两件事：

- **`ctx.on('事件名', ...)`**：监听别人的事件，用四种方式参与——`emit`（围观）、`waterfall`（拿到内容可以改、可以拦）、`parallel`（并行干活）、`serial`（排队按序）；
- **`ctx.tools.register(...)` 这类调用**：往别人的注册表里塞东西——加一个工具、加一段提示词、加一个模型适配器、加一个守卫。

上面的 130 行例子就是这种：它不定义任何服务，纯靠监听 `fs/*` 事件，就实现了"编辑文件前必须先读过"这条规矩。压缩（compaction）、审批策略、超大结果落盘（spill）全是这个路数。

### ③ 写插件，造新能力系统（占一个服务位）

在 `apply(ctx)` 里 `ctx.provide('werewolf', ...)`，你就发明了一个新能力：它有自己的名字（`ctx.werewolf`）、自己的接口、自己的事件，**和 `ctx.tools`、`ctx.agent`、`ctx.llm` 平起平坐**。

仓库里现成的例子：`ctx.goals`（目标系统）、`ctx.agentTeams`（多 agent 协作）、`ctx.jobs`（后台任务）、`ctx.workflowEngine`（工作流引擎）——每一个都是"有人觉得缺这个能力，就造了一个"。你要做"看视频 Service"、"AI 玩狼人杀"，就是这一档：定义接口 + 事件，别人还能反过来消费你。

### ④ 最深一层：扩展"词汇表"本身

往系统的**基础类型**里加新条目：

- 往 `SessionEventMap` 加一种新事件 → 账本多了一种记账科目，模型可见、可回放；
- 往 `ContentBlockMap` 加一种新内容块 → 消息里能出现新形态的内容。

这比造 Service 更深：你在改系统的"数据语言"，而且是用追加的方式改，不动别人的代码。

---

## 一张表总结

| 档位 | 要写代码吗 | 占服务位吗 | 一句话 |
|---|---|---|---|
| ① 配置 patch | ❌ | ❌ | 换掉/关掉/重配任何已有插件 |
| ② 挂别人 | ✅ 小 | ❌ | 监听事件、往注册表塞东西 |
| ③ 造能力 | ✅ 中 | ✅ `ctx.xxx` | 发明一个和 tools/llm 平级的新系统 |
| ④ 改词汇 | ✅ 小 | ❌ | 给账本加新事件类型、给消息加新内容形态 |

四档可以混用：一个插件可以同时造 Service、监听别人的事件、还往 `ctx.tools` 注册工具。

**唯一的铁律**：只许依赖接口（Service Definition），不许依赖具体实现。所以替换任何实现都不会波及别人——这就是"换执行环境不用改工具代码"的根本原因。

> 备注：仓库还有第五档（agent 运行时自己写插件、自己挂载），我们不采用——不可控、不可持续，Go 版直接不做。
