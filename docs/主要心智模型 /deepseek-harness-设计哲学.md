# DeepSeek Harness（dsh）设计哲学 — Go 重写参考

> 依据 `reference/deepseek-harness` 的文档和核心源码整理。
> 目标：讲清楚"这个项目到底是怎么设计的、为什么这样设计"，以及我们用 Go 重写时该怎么做。

---

## 0. 一句话总结

**dsh = 一本"流水账" + 一条"流水线" + 一堆"标准插座"。**

- **流水账**：agent 和模型、工具的所有互动，都先记到一本只增不改的账本（事件日志）里。模型看到的历史、UI 显示的内容、崩溃后的恢复，全部从这本账"算"出来，没有第二份事实。
- **流水线**：一次工具调用要过五道关卡（审批、守卫、执行、复盘、定稿），每道关卡都是别人可以插手的标准接口。
- **标准插座**：文件系统、shell、LLM、子 agent……每一项能力都是一个插座（接口）。想换成本地执行还是远程沙箱？拔掉插头换一个就行，上层工具完全无感。

再加上一句灵魂口号：**这个项目里没有任何东西是"特殊的"**。连 agent 主循环本身都只是一个插件，可以被配置文件整个换掉。

---

## 1. 插件体系："一切皆插件"是怎么做到的

### 1.1 核心机制

每个功能都是一个**插件**，插件做的事情只有一件：**向一个共享的"服务台"登记自己**。

- 每个服务在服务台上有个固定名字（如 `ctx.tools`、`ctx.llm`、`ctx.sessions`）。
- 别的插件要用这个服务，就按名字去拿，**绝不直接 import 别人的实现代码**。
- 登记的东西（工具、监听器、适配器）都是"可撤回的"——插件卸载时，它登记过的一切自动消失。

打个比方：这就像一个插件版的 Spring/DI 容器，但更极致——不仅服务是插件，**主循环、持久化、模型适配器全都是插件**，所以整个产品的每个零件都能单独替换。

### 1.2 四种事件模式（重要！）

插件之间通过"事件"沟通。每种事件有固定的**播报方式**，这是公开契约：

| 模式 | 大白话 | 典型用途 |
|---|---|---|
| `emit` | 广播："这事发生了，随你们怎么看"。喊完就走，不等 | 通知 UI "状态变了" |
| `waterfall` | 击鼓传花："这个东西经过你手，你可以改，也可以直接拦下" | 权限审批、改写请求 |
| `parallel` | 全员同时干活，等大家都干完 | 落盘前的持久化屏障 |
| `serial` | 排队一个个来，按顺序等 | 轮次结束前的最后检查 |

其中 **waterfall（瀑布式）最常用**：每个监听器拿到 `(内容, next)`。调用 `next()` 就是"我没意见，传给下一个人"；不调用直接返回结果，就是"我有意见，到此为止"。这就是经典的中间件模式（类似 Express/Koa 的洋葱模型）。

### 1.3 组装：像叠衣服一样叠配置

最终跑起来的程序是一棵插件树，按顺序一层层叠出来：

```
空配置
 → 组合包1（基础能力：模型、工具、持久化……）
 → 组合包2（比如 web-app 或 headless）
 → 用户自己的 patch 文件（按 id 整体替换任意条目）
```

关键点：**上层 patch 可以替换下层的一切**。`dsh --dump-config` 能打印出当前完整配置树，里面任何一条你都可以用自己的 patch 换掉。这就是"没有特权内核"的含义——不存在改不动的地方。

---

## 2. Scope（作用域）：每个 agent 可以有自己的"私人定制"

这是个容易被忽略但很精妙的设计，规则出奇地简单：

- 任何注册的东西（工具、提示词片段……）只有两种归属：**全局的**（所有 agent 可见）或**属于某一个 agent 的**。
- **只有两层，没有多层继承！** 给 agent A 定制的工具，A 的子 agent 看不到。
- 同名时，agent 自己的注册**遮蔽**（shadow）全局的——这就是"给某个 agent 换个专属 persona 或工具变体"的机制。

那子 agent 怎么继承父 agent 的能力？答案是**绑定到同一个已组装好的实例**（`composeFrom`）：子 agent 直接共用父 agent 当时启动的那批插件对象。为什么不按名字重新加载？因为如果父启动后配置文件被改了，重新加载会让父子俩用上**不同版本**的工具集，历史就对不上了。

另外两个要点：

- **父子关系是数据，不是结构**：谁创建的谁、委派深度多少，这些信息跟着消息走，不影响任何可见性。
- **restrict（限制）只裁别人给的，不裁自己的**：一个 agent 可以过滤掉继承来的全局工具，但它自己注册的工具不受影响——被委派出去干活的子 agent 得保住它回报结果所依赖的工具。

---

## 3. 会话日志：整个系统的"灵魂"

### 3.1 基本设计

`Session` 就是一本**只增不改的流水账**，每记一笔叫一个**事件**（event）：

- 每笔带一个递增编号（`seq`），必须连续；
- 内容必须是**纯 JSON**（能无损存取）——记的时候就检查，不合格直接报错，绝不留到写盘时才发现；
- 记进去就冻结，谁也改不了历史。

典型的账目有：轮次开始/结束、步骤开始/结束、用户消息、模型的原始流式片段、模型的完整回复、工具调用、工具结果……约 12 种核心事件，插件还可以加自己的"只记账"事件。

### 3.2 关键理念：模型看到的 = 账本里有的

模型每次请求看到的对话历史，不是单独存的，而是**从账本算出来的**（`deriveMessages()`）。这条铁律带来一堆好处：

- **崩溃恢复**：重启后重放账本即可，什么都不会丢；
- **回放 UI**：界面显示的就是账本内容的另一种投影；
- **fork 分叉**：把账本复制到某一行，就是一次分叉；
- **压缩（compaction）**：见下。

反过来的推论也成立：**想给模型看任何新东西，就必须发明一个新事件类型**。系统里有个运行时检查专门断言这一点——模型请求里的每个字节都能从账本重建。

### 3.3 Surface 机制：压缩是怎么做成"插件"的（全项目最巧妙的一笔）

问题：对话太长了要压缩总结，但账本"只增不改"，怎么"删掉"旧消息？

答案：三种会产生消息的事件（用户消息、助手消息、工具结果）可以带一个 **surfaceOp 标记**：

- `append`：正常追加到对话末尾；
- `replace (start, end)`：**"从第 start 到第 end 条，被我这一条概括取代了"**。

账本本身一个字都不删——replace 只是个标记。算模型历史的时候，被标记取代的旧消息就被跳过了。压缩插件做的事就是：发一个摘要事件，标记它取代了哪些旧消息。完事。

还有个 `replaceGeneration`（替换代数）计数器：每次 replace 发生就 +1，缓存看到代数变了就知道"历史被重写了"，整体重算一遍，否则只需算新增的。

> 细节：人类看的对话记录（transcript）读的是**全部追加事件**，所以人仍能看到被压缩前的完整历史——模型和人类看的是同一本账的不同投影。

### 3.4 连"请求"本身都记账

每次发给模型的请求信封（用什么模型、什么参数、渲染好的系统提示词、工具列表）也作为事件记进账本（`request/header`）。效果是：**任何一次模型请求都是账本的纯函数**——给它同一本账，发出去的请求一模一样。排查问题、回放、复现都因此变得 trivial。

### 3.5 落盘：异步、不挡路

记账（append）是纯内存操作，**从不等硬盘**。持久化插件在旁边订阅事件流，攒一批慢慢写。真需要"确定已落盘"的地方（比如轮次结束检查点），显式调用 `flush()` 等一下。崩溃恢复时，只把确实处于"开放中"状态的尾部轮次标记为 `interrupted`，绝不乱补。

---

## 4. Agent 与 Inbox：消息进来的三种方式

`Agent` 是大家（UI、钩子、编排器）操作一个活跃 agent 的把手。给它发消息有三种语义，区别只在于**放进哪个收件箱 + 要不要叫醒它**：

| 方法 | 放哪 | 叫醒？ | 大白话 |
|---|---|---|---|
| `followup()` | 下一轮收件箱 | ✅ | "用户说话了，开一轮新对话" |
| `steer()` | 下一步收件箱 | ✅ | "正在跑也别停，下一个步骤把这话捎上"（中途转向）|
| `inject()` | 下一步收件箱 | ❌ | "下次模型请求时带上这段上下文，但现在别吵它" |

妙处在于收件箱本身也是**持久化投影**——收件箱的每次插、删、认领都记成账本事件，所以重启后"还没处理的消息"不丢。

几条踩坑换来的铁律：

- **`followup()` 不返回结果**。想知道"这条消息的结果"是没有意义的——队列里可能攒了好几条消息共用一个执行区间。要等就等 `whenIdle()`（整个 agent 空闲），但即使这样也**不能**把之后的输出归因于你那条消息。官方文档专门写了一节警告这个。
- **取消是带原因的**（用户取消/父级取消/钩子取消/销毁），但持久化的轮次结果里只记粗粒度的"被中止了"——细节留在实时层。
- **维护任务**：空闲期可以插入"维护任务"（比如攒标题），对外状态仍显示 idle，新消息会在收件箱里排队等它干完。

---

## 5. 主循环：一轮对话的完整旅程

先定义两个词：

- **步骤（step）** = 一次模型请求 + 它引发的所有工具执行；
- **轮次（turn）** = 从接到输入开始，到模型不再欠任何工作为止，包含零或多个步骤。

一轮的旅程：

```
turn/start 开账
  ① 从收件箱认领消息（全部的"下一步"消息 + 一条"下一轮"消息）
  ② pre-step 检查站（waterfall）：监听器可以改写这批消息，也可以直接拒绝
     —— 即使被拒绝，也会记一个"没有步骤的空轮次"，让账本留下这次尝试
  step/start
  ③ 组装请求：系统提示词（各插件贡献的片段拼的）+ 工具列表 + 从账本算出的历史
  ④ 请求检查站：监听器只能换模型/参数，不能偷改消息内容
     （想给模型看东西？走 inject，老老实实记账去）
  ⑤ 流式收模型输出：每个片段实时记账（所以界面能逐字显示）
     —— 中途被取消？已收到的部分作为"被中断的消息"记入账本
  ⑥ 完整回复记账（并注明它由哪些流式片段组装而来）
  ⑦ 执行工具调用（见下一节），结果逐个记账
  step/end
  ⑧ 还有欠的工作吗？（工具要求再问一次模型 / 收件箱又来了新输入）
     有 → 回到 ②，进入下一个步骤
     没有 → turn-stopping 最后检查站：想挽留？注入一条 steer 消息即可
  turn/end 结账
```

几个值得注意的设计决策：

- **错误恢复也是插件**：模型请求失败后，走 `request-error` 检查站。压缩插件在这里干活——如果是上下文超长，就先剪枝旧工具结果、再做摘要压缩，然后说"重试"；其他错误默认终态。
- **中途转向的待遇**：steering 消息和普通输入走同一个 pre-step 检查站，一视同仁。
- **max-tokens 是"粘性"的**：一轮里只要有一个步骤被截断，整轮的结局就标记为截断，后面即使正常完成也不覆盖这个事实。
- **工具调度**：声明了"并发安全"（`isConcurrencySafe` 精确返回 true）的调用组成滚动并行池；没声明的形成独占屏障，单独跑。每组开跑前重新分类（工具注册可能刚变过）。中途取消时，没跑到的调用补记"被跳过"（不留悬空的调用记录）。

---

## 6. 工具流水线：一次工具调用的五道关卡

模型说"帮我执行 `rm -rf /tmp/foo`"之后发生的事：

```
① 记账 tool/call（参数原样记录，哪怕是非法 JSON 也原样存）
② pre-execute 检查站（waterfall）：allow / deny / ask（需要人工审批）
③ 审批：弹给用户。注意——没有任何人负责回答时，默认【拒绝】，不是放行
④ 单调守卫（guard）：最后一道防线。
   关键设计：守卫只能 deny，不能 allow！
   —— 所以不存在"监听器排序诡计把禁止的事情翻回允许"
⑤ execute 环绕层（waterfall）：超时、重试、指标都在这里包一层
⑥ 工具本体执行：只能返回纯 JSON 的"规范值"，由声明好的 render 函数投影成模型内容
⑦ post-execute 检查站：可以接受 / 替换结果 / 附加上下文 / 阻止并给纠错反馈
⑧ 定稿：所有异常都规范成 isError；最终结果冻结，广播 tools/result，记账
```

贯穿始终的三条原则：

1. **身份不可伪造**：调用 ID 和冻结的参数贯穿全流程并进入持久记录——任何包装层都无法制造"另一个版本"的这次调用。**参数谁都改不了**：账本、审计、UI、执行看到的必须是同一份。
2. **取消拆不掉**：环绕层可以换掉取消信号，但流水线会把调用方的原始信号"重新焊回去"。
3. **规范值不落盘**：工具内部的中间值只活在执行期间，账本只记最终呈现内容。回放能重现"看到了什么"，不假装能重现"内部算过什么"。

另外还有个贴心设计：工具可以提供**纯函数的 UI 渲染意图**（`presentCall`/`presentResult`，返回"终端卡片/diff 卡片/搜索卡片"等）。因为只依赖参数、无副作用，实时流式和日志回放都能调用——UI 不需要为每个工具写专门逻辑。

---

## 7. 能力插座（Seam）：为什么"换个沙箱"这么容易

一项能力的完整定义叫一个 **seam**，固定三个角色：

```
接口定义包（Service Definition）  ←  只描述"能做什么"
    ↑ 被实现              ↑ 被消费
实现包（Provider）         使用方（Consumer，通常是面向模型的工具）
```

规则：**扩展插件只允许依赖"接口定义"，绝不依赖"实现"。** 规范例子：`dsh-shell`（接口）+ `dsh-bash-local`/`dsh-bash-sandbox`（实现）+ `dsh-tool-bash`（消费方）。

这个模式的威力在"**执行世界**"的收敛上：文件系统插座（`ctx.fs`）和子进程插座（`ctx.subprocess`）可以指向**同一个后端**。所以把它们俩换成远程沙箱（E2B）的提供方后——Bash、终端、LSP **全部自动**搬进了沙箱，一行上层代码都不用改，也不需要 fork 任何工具。

子 agent 同理：一个接口后面有六种实现（进程内新建/分叉、ACP 协议、Codex、Claude Code、SDK……），模型眼里的"委派工具"完全不变。

---

## 8. 防御性设计：踩过的坑变成的规矩

这些每一条都对应真实或险些发布的缺陷，Go 版直接照做：

1. **独立事实分开报**：进程可能"超时了但退出码是 0"（它自己捕获了超时信号）。所以超时、信号、退出码各报各的，绝不互相嵌套判断。
2. **dispose 要"等到停稳"，不是"喊一声就走"**：清理必须 await 子进程真正退出，否则留孤儿进程。先关监听器再杀进程，让迟到的完成事件无处可去、保持安静。
3. **一个坏监听器不许坏事**：分发循环必须逐个捕获监听器异常——它不能让主流程失败，也不能饿死排在后面的监听器。
4. **环境与路径卫生**：传给子进程的环境变量里清掉名字带 KEY/SECRET/TOKEN/PASSWORD 的；临时/溢出文件放 0700 私有目录、随机文件名、独占创建。
5. **删符号链接只用 unlink**：递归删除可能穿过链接进到目标目录，把别的东西删了。
6. **错误要结构化**：任何异常最终折平成 `{message, code}`，不许裸字符串到处飞。

---

## 9. 值得整体照抄的不变量清单

写 Go 版时，把这些做成**运行时断言 + 测试**，而不是口头约定：

- 模型可见即已记录：模型请求的每个字节都能从账本重建。
- 记账时校验：JSON 合法性、编号连续性、surface 标记合法性——坏数据在源头报错。
- 被拒绝的 pre-step 也记一个"空轮次"——账本要留下尝试的痕迹。
- 权限只收不放：守卫没有 allow 结果；审批无人应答即拒绝。
- 参数与调用身份全程不可变；取消信号可以换但拆不掉。
- 扩展只挂到已文档化的扩展点（事件/插座），不许 fork 内核。

---

## 10. Go 实现蓝图

### 10.1 插件容器（对应 Cordis）

```go
type Context struct {
    services map[string]any  // 服务表：名字 → 实现
    cleanups []func()        // 清理栈：逆序执行
    parent   *Context
}

func (c *Context) Provide(name string, svc any)   // 登记服务
func (c *Context) Effect(cleanup func())          // 登记可撤回的副作用
func Service[T any](c *Context) T                 // 泛型按键取服务
```

- 插件 = `Apply(ctx *Context) error`；依赖用接口字段声明，启动时拓扑排序（有环即失败）。
- **Scope**：`ScopeKey` 就用 `any`（interface 按身份比较）；注册表做成"全局层 + 按需创建的作用域层"，两层扁平 + 遮蔽 + 限制交集，照抄第 2 节的规则。
- TS 的"声明合并"不需要——事件词汇就是 Go 常量；遇到**未知事件类型默认拒绝重建**会话（宁可过严，不可静默丢）。
- 组装：配置条目列表（id + config + 挂载函数），profile/bundle/patch 逐层**整体替换**，支持 `--dump-config`。

### 10.2 事件总线：四种模式做成四个方法

```go
Emit(name, payload)                          // 广播
Waterfall[T](name, payload, next func() T) T // 击鼓传花
Parallel(name, payload) error                // errgroup 并行
Serial(name, payload) error                  // 按序
```

分发器统一 `recover`，把监听器 panic 隔离成日志（第 8.3 条）。权限类事件返回类型化 Decision 枚举，只收不放。

### 10.3 会话日志（重头戏，设计照搬）

- `Append(kind, data)`：先 `json.Marshal` 验一遍 → 深拷贝快照 → `seq = len` → surface 校验 → 同步发布给观察者（观察者 panic 被隔离）。Go 里不需要 deep freeze，拷贝快照天然不可变。
- Surface：`[]int`（存活消息的 seq 列表）+ 替换代数计数器；`Replace{Start, End}` 对应压缩；`DeriveMessages()` 增量缓存，代数变了就整体重建。
- `request/header` 照搬：请求是账本的纯函数。
- 持久化做成插座：JSONL 先行，订阅事件流异步缓冲，`Flush()` 走 Parallel；崩溃恢复只关确实开放的尾部轮次。

### 10.4 Agent 与收件箱

- 接口：`Send / Followup / Steer / Inject / Cancel / WhenIdle / RunMaintenance`；两条有序收件箱，每次变更记规范化 splice 事件。
- 取消：`context.Context`，类型化 cause 存在自定义 cancel 函数里。
- 创建事务：setup 回调失败整体回滚；会话的清理放进 agent 的同一条清理链（保证驱动器的收尾事件先发布、会话钩子后撤——这是他们专门修过的竞态）。

### 10.5 工具流水线

- `ToolDefinition`：Schema + 输出契约（Schema + Render 纯函数）+ `Execute(args, exec)` + 可选的 FinalizeContent / IsConcurrencySafe / TimeoutMs / UI 渲染。
- 五道关卡顺序固定：pre-execute → 审批 → 单调守卫 → execute 环绕 → post-execute → 规范化 → 定稿广播。
- 并行调度：滚动池 + 独占屏障，goroutine + channel 或 errgroup 实现；abort 后剩余调用补记"跳过"。

### 10.6 LLM 插座

- `Adapter` 接口 + 流式分片判别联合（Go：kind 字段 + type switch）。
- **规范化边界照搬**：适配器可以返回 error，但 runtime 层必须把它转换成终止型 finish 分片——消费方永远不用猜"这个 error 是谁的锅"。
- `PrepareCall`（预备调用）照搬：配置解析和适配器绑定一次完成、全部冻结、只能 dispatch 一次，配置变了就报错——保证账本里记的请求头和实际分发的一致。

### 10.7 实现顺序（每步带不变量测试）

1. 容器 + 事件总线 + scope 原语
2. 会话日志 + surface + 历史投影（配回放/不变量测试）
3. 工具注册表 + 五道关卡流水线
4. 系统提示词组装 + LLM 插座 + 第一个适配器
5. agent 主循环 + 收件箱 + 取消语义
6. 能力插座逐个铺：fs → shell/子进程 → 沙箱 → 子 agent → ……
7. 持久化（JSONL 先行）、web 入口、profile/bundle 组装

> 提醒：dsh 的 `test-support` 包里有一整套 testkit、回放、不变量检查的基础设施——**测试是这个项目的一等公民**，不是附属品。照这个标准做，Go 版的质量才有保障。
