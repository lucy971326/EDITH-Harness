# 后续计划

这里只记录尚未完成的工作。已完成事实见仓库根 `STATUS.md`，产品形状见 `docs/设计书.md`。做完的计划不留在 `docs/plan/`。

## v1 之前

### 1. 收尾 Chat 已有能力

- 将 Chat 的 demo sidepanel 替换为真实工作区文件树与文件查看面板。

### 2. 扩展输入

- Chat `/` 的 MCP 展示仍未实现。
- 在真实输入需求出现后，扩展 `composer.actions` 支持多模态输入；先定义输入的落盘归属和模型适配范围。

### 3. 多 Agent 与可追溯性

- 实现 [Subagent 系统](subagentsystem.md)：异步委派、多次交流、完成通知与等待唤醒；孩子的完整过程与业务状态不写入父 Session。
- Subagent、MCP 已产生足够真实事实后，再实现轨迹页。

### 4. 完整验收

- 验证产品切换、SSE 重连、慢客户端、取消和关闭。
- 完成与改动匹配的 Go、race、vet、跨平台编译与真实浏览器验收。

## v1 之后再评估

- Go SDK 与仓库外插件验证；出现外部用户再发布插件开发 Skill。不做动态插件和市场。
- 电影、狼人杀与更多面板只在真实需求出现时实现。
- 不为了展示未来方向提前创建空目录或抽象接口。
- 「所有产品统一 Store」不是已排期功能；等第一个真实产品需要独立持久状态时再定。
