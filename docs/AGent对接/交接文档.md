# 交接文档 —— edith-harness（Go 版 agent 框架）

> 写给接手的 AI。本文自包含：读完后你应该能不动摇地继续 M6 施工。
> 人类用户是 Lucy。她要大白话，不要黑话；她逐行 review 代码，说"提交"才提交。
> 最后更新：2026-08-24，M6 已有 JSONL 账本、持久 Agent 档案、多会话恢复、DeepSeek 真适配器、YAML 组装和可用终端 UI。

---

## 0. 一句话认识这个项目

本项目叫 **edith-harness**，用 Go 重写 DeepSeek 开源 agent 框架（dsh，TypeScript）的对应物：**一个小而稳的内核 + 一切能力靠插件**。module 名仍是 `harness`，Go 1.25.5，单二进制、静态编译；首个真模型插件使用官方 OpenAI Go SDK。

进度：**M1-M5 完工；M6 已完成 JSONL 账本、Agent 档案、旧会话打开、DeepSeek 真适配器、YAML 组装、终端交互闭环和 `--dump-config`。尚待带真实密钥做人工烟测。**

施工方式是里程碑制：每一节先读计划书 → 写代码 → `-race` 全绿 → 用户 review → 用户说"提交"才 commit。

---

## 1. 必读文件（按这个顺序读）

**注意：文档刚搬过家，以下全部是新路径。**

| 路径 | 是什么 | 什么时候读 |
|---|---|---|
| `AGENTS.md` | 施工守则（代码风格/注释规范/提交规范，全部钦定不许漂移） | 开工前，必须背下来 |
| `失败边界与恢复状态机.md`（项目根目录） | 红队终审交付物：崩溃恢复的唯一依据，一张状态表 | M5/M6 动手前必读 |
| `docs/计划书/Go框架设计构想.md` | 总设计：命名、代码组织、五大模块、明确不做的事 | 第一份读的设计文档 |
| `docs/计划书/M1-core计划.md` … `M6-组装冒烟计划.md` | 六个里程碑计划书，全部大白话写成 | 做哪节读哪节 |
| `docs/主要心智模型 /deepseek-harness-设计哲学.md` | dsh 源码的设计哲学解读 | 想理解"为什么"时 |
| `docs/主要心智模型 /dsh-插件是什么.md` | dsh 插件四档扩展模型 | 写插件时 |
| `docs/主要心智模型 /kernel心智模型3D.html` | 3D 可视化（浏览器打开看） | 随意 |
| `reference/deepseek-harness/` | dsh 原始源码 | **只许看，不许抄进本项目** |
| `docs/AGent对接/交接文档.md` | 就是本文 | — |

**两个坑，先记死：**

1. 目录 `docs/主要心智模型 ` **名字末尾有一个空格**（历史遗留），写路径时别丢了。
2. `AGENTS.md` 里"施工依据"一节引用的还是搬家前的旧路径（如 `Go框架设计构想.md` 在根目录），实际文件都在 `docs/计划书/` 下。接手后可顺手修正，改前知会用户。
3. 这次文件搬家**还没有 commit**（git 里表现为根目录一批删除 + `docs/` 未跟踪）。接手后第一件事建议就是把这批移动提交掉。

---

## 2. 核心理念（五条命根子）

所有代码决定最终都回到这五条。遇到没写过的情况，拿这五条裁决。

### ① 内核做小，做完不改

内核一共就几个包：`core`（场地）、`chat`（词汇）、`session`（账本）、`llm`（插座排）、`tools`（流水线）、`loop`（搬运工），加上 M5 的 `files`/`process`/`shell` 和 M6 的 `cmd/edith-harness` 组装。**M6 做完 = 内核完工，之后一切新能力都是插件，内核不再为新能力改一行代码。**

### ② 账本是唯一事实源

`session` 包记的账（事件流）是整个系统唯一的事实：

- 模型看到的对话历史 = 从账本**算出来**的，不单独存；
- UI 显示 = 账本的另一种算法；
- 崩溃恢复 = 把账重放一遍；
- 上下文压缩 = 追加一笔摘要事件（带 `Replaces` 声明"我取代了哪几笔"），旧账不删；
- **验收红线：模型收到的每个字节都能从账本重建。**

账本规矩极硬：只增不改；编号从 1 连续；落盘要么整条要么没有；危险操作（调模型、执行工具）之前必须 `Flush`（把攒的账写穿到硬盘）。

### ③ 宁可说不知道，不可自动赌

工具执行到一半崩溃（账上有 `tool/start` 没有 `tool/result`）= 副作用可能已经发生（邮件可能已发出）。此时**绝不自动重跑**（赌错 = 双发），也**不卡死等人**——自动补一笔"结果不明"的账给模型，让模型自己决定重做还是问人。这条是用户亲自拍板的"路线 2"，详见 `失败边界与恢复状态机.md`。

### ④ 插件只依赖接口，不依赖实现

所有能力挂在公共能力表上，键是**能力名**不是插件名：`"llm"`、`"tools"`、`"agents"`、`"sessions"`、`"approval"`。谁提供实现随便换（本地换沙箱、DeepSeek 换 OpenAI），用的人无感。

### ⑤ 显式优于聪明

- llm 请求必须明确带 Provider、Model、Thinking，不猜默认服务商；
- 工具要问人（ask）但没人挂 `"approval"` 能力 = 拒绝执行，不默默放行；
- 插件安装中途失败 = 逆序回滚已启动的，不留半装状态；
- `Resolve[T]` 查不到就报错，类型不对直接 panic（编程错误就该炸）。

---

## 3. 拍板过的重要决定（完整清单）

每条都定过了，**不要重新发明**。要翻案必须先问用户。

### 3.1 架构裁决

| # | 决定 | 理由 |
|---|---|---|
| 1 | **不做**：动态加载插件、热重载、AI 自我修改、Web 前后端分体 | 静态编译单二进制，可控可测 |
| 2 | 事件分发有四种，**各自失败语义不同**：`Broadcast` 崩溃隔离（观察者炸了只记日志）/ `RunChain` 错误上抛 / `RunSequentially` 出错中断 / `RunConcurrently` 聚合所有错误 | 不许一刀切 |
| 3 | `Broadcast` 是同步分发，不起 goroutine | 防流式输出触发 goroutine 风暴；要异步的监听器自己声明 |
| 4 | 服务表键 = 能力名，不是插件名 | 换实现不影响消费方 |
| 5 | 作用域 = 子 `App`：全局 → Agent → 会话；同名遮蔽、`Restrict` 只能裁继承来的、子事件上浮到父的观察者（不下沉）、子 `Close` 后整批消失 | Agent 的工具/人格可被多会话共用，会话状态互不串台 |
| 6 | `chat` 包是中立词汇表（Message/Attachment/ToolCall/ToolResult/Usage/Delta/ToolSchema），`session` 和 `llm` 都用它，**两个包互不依赖** | 谁也不借谁还没写出来的类型 |
| 7 | 多模态：`Message.Media []Attachment` 留了口子（图片/音频输入）；**生图/生视频是将来插件的事，不在内核** | 内核只管词汇，不管能力 |
| 8 | `llm.Adapter` 只有**一个**方法 `Stream`；非流式 = 不吐 delta 的特例；ctx 取消翻译成 `cancelled` 错误 | 适配器只需实现一种形状 |
| 9 | 统一错误七种 Kind：network / rate_limit / auth / bad_request / server / cancelled / unknown | 消费方可编程处理 |
| 10 | 工具执行是**九步流水线，顺序冻结**（见 §4.5），改顺序 = 破坏性变更 | 顺序本身就是契约 |
| 11 | 控制位（守卫/审批/重试）挂在 tools 的公共链上，**不是**各工具自己实现 | N 个工具 × M 种控制：挂公共路径写 M 次，挂工具里写 N×M 次 |
| 12 | 审批是**可选插件**（挂 `"approval"`，实现 `Approver` 接口），内核不内置问人 | 内核不该知道"人" |
| 13 | 工具表两层：全局工具代码 + Agent 档案的工具白名单；最后一段会话关闭才 `DropAgent` | 见决定 5 |
| 14 | 每种账本事件**只归一家记**：`session` 提供 14 个专用 `Record*` 方法；公开的 `AppendEvent` 只许插件记自定义 Kind | 防记错格式 |
| 15 | 投递和领出是**两笔账**：消息先记 `inbox/deliver` 再入队，领出时再记 `inbox/claim`，**领出才变成进模型的 user 消息** | 崩溃恢复只重放未领出的，消息不双份不丢失 |
| 16 | 忙时碰撞：`Steer`（中途话）挤进当前轮下一步；`SubmitFollowup`（新话）排队，本轮收尾后才开新轮；`InjectMemo`（小抄）立刻入账但不唤醒、不占待办 | 用户终审过的语义 |
| 17 | 崩溃后"有 start 无 result"的工具调用 → **自动补 `tool/result{status:"unknown"}`**，正文写给模型的决策指引；不自动重跑、不卡死 | 路线 2，用户拍板 |
| 18 | `loop` 本身是插件，可整体替换；UI 只认 `"agents"` 能力，不知道 loop 内部长什么样；显示过程订阅 `session/appended` | 门面模式 |
| 19 | `profilejson.Plugin` 先登记 `"agent-profiles"`；Journal 插件登记 `"journal"`；`session.Plugin` 创建并登记 `"sessions"`；loop 最后领取四项能力 | 两种存储介质都可替换，session 仍是不能省略的地基 |
| 20 | 事件里 `SkipIfUnknown` 字段：true = 写这笔的插件将来没了就跳过；false = 整本账拒读 | 宁可过严 |

### 3.2 代码风格裁决（全部在 AGENTS.md，这里点重点）

- **err 的获取和判断不许写同一行**：先 `err := f()`，下一行 `if err != nil {`。用户反复强调，违者重写。
- 禁止花里胡哨的语法；**要全局可读性**，不是单个函数的可读性。
- 方法名自解释动词（`SubmitFollowup`/`ExecuteCall`/`AppendEvent`）；包名领域词；不发明概念名（Kernel/Ledger/Fiber/Realm 出现即毙）；避开标准库撞名。
- 一包一个公开构造；插件 = 结构体 + `Start(app)`，不用闭包式插件；闭包只做无状态包装。
- 注释：结构体一句话说组合了什么 + 核心字段标注角色（小能力/身份/长期配置）；函数 = 输入+输出+一句话干嘛+失败语义；内部注释纯中文；言简意赅不滥用。

### 3.3 流程裁决

- 每节动工前**先读对应计划书**。
- `go test -race` 全绿才算完工。
- **完成不自动提交**：用户 review 后说"提交"才 commit。
- commit 说明**双语**：第一行中文摘要（50 字内），空一行，正文英文（干了什么/为什么/怎么验的）。用 heredoc `git commit -F -` 防 shell 引号事故。
- 跟用户说话：**大白话，禁黑话**；问什么答什么，一句话答完不扩展。她看不懂就等于没说。

---

## 4. 代码地图（逐文件）

```
harness/
├── core/       场地：谁都能来的公共大厅
├── chat/       词汇：对话的数据形状
├── session/    账本：发生过的一切
├── llm/        插座排：调模型的统一口
├── tools/      流水线：模型的手
├── toolplugins/  具体工具插件（一类工具一个目录）
│   └── files/     文件工具：当前只有 write_file，以后可加 read_file
├── loop/       搬运工：不知疲倦的主循环
├── persistence/ 持久化插件（一种介质一个目录）
│   └── jsonl/     JSONL Journal：一行一条 JSON + fsync + 烂尾修复
│   └── profilejson/ Agent 档案：一档一 JSON 文件，原子替换
├── workspace/ 工作空间能力（这三个都不是插件）
│   ├── files/     Store 接口 + Get(app)
│   ├── process/   Spawner / Handle 接口 + Get(app)
│   └── shell/     借任意 process 跑完一条命令
├── localenv/  本地文件 + 本地进程实现，一次装上面三块
├── compose/   读取 config.yaml、选择大零件、按固定顺序安装
├── ui/        UI 的极小启动口：只规定 Run(ctx)
├── uiplugins/ 具体 UI 插件
│   └── terminal/ Bubble Tea TUI：选 Agent / 会话、发消息、显示过程
└── cmd/edith-harness/   找到用户目录和工作目录，调用 compose.Open 后运行 UI
```

工作空间这条依赖也是单向：`core ← workspace/files`、`core ← workspace/process ← workspace/shell`；`localenv` 只负责组装三者。

### 4.1 core/（零依赖，不懂任何 agent 知识）

| 文件 | 职责 | 关键符号 |
|---|---|---|
| `core/app.go` | `App` 结构：服务表/监听器表/中间件表/任务表/清理栈；`New()`；`Plugin` 接口（`Name()` + `Start(app)`）；`Install`（按序启动，失败逆序回滚）；`RegisterService`（重名 panic）；`Resolve[T]`（缺了报错、类型错 panic）；`OnCleanup`；`Close`（逆序清理、清表、幂等、崩溃隔离） | `App`, `Plugin`, `Install`, `Resolve[T]`, `Close` |
| `core/events.go` | 四种分发（`Broadcast`/`RunChain`/`RunSequentially`/`RunConcurrently`）；`Subscribe`/`Intercept`/`RegisterTask`；`RunChain` 是洋葱折叠，中间件拿 `next(P) R` 可改写载荷和结果 | `Listener`, `Middleware[P,R]`, `RunChain` |
| `core/scope.go` | `ForAgent(name)` 派生子作用域；`Restrict(names...)` 裁继承；`Parent()` 访问器 | `ForAgent`, `Restrict` |

### 4.2 chat/message.go（纯数据，无测试）

`Message{Role, Text, Interrupted, Media []Attachment, Calls, Result}`、`Attachment{Kind, URL, Data, Mime}`、`ToolCall{ID, Name, Argument}`、`ToolResult{CallID, Output, Status}`、`Usage`、`Delta`（流式一小截）、`ToolSchema{Name, Description, Parameters []byte}`。

### 4.3 session/（账本）

| 文件 | 职责 |
|---|---|
| `session/types.go` | `Event{Kind, Seq, Time, Data json.RawMessage, Replaces []int, SkipIfUnknown bool}`；`Header{FormatVersion=2, ID=SessionID, AgentID, ProfileRevision}`；14 个内核 Kind 常量；请求快照保存当年真发的 `llm.Request` JSON |
| `session/journal.go` | `Journal` 接口：`Create` / `Append(id, event, durable)` / `Flush(id)` / `ReadAll`。规矩：要么整条要么没有；durable=true 成功返回前必须真到硬盘；`MemoryJournal` 只给测试 |
| `session/codec.go` | 事件格式注册表：每种 Kind 登记 modelVisible（进不进模型历史）/ kernel（只许专用方法记）/ decode；`isDurable`：chunk 以外全部立刻落盘 |
| `session/session.go` | `Session`：14 个专用记账方法（`RecordUserMessage`/`RecordChunk`/`RecordAssistantFinal(final, replaces)`/`RecordToolCall`/`RecordToolStart`/`RecordToolResult`/`RecordTurnStart-End`/`RecordStepStart-End`/`RecordDeliver(id,text,target)`/`RecordClaim`/`RecordSnapshot(requestJSON)`/`RecordSummary(data,replaces)`）；广播：`EventAppended="session/appended"`，负载 `Appended{SessionID, Event}`；插件自定义事件在广播回调里走 `AppendEvent` 会被拒（`ErrInBroadcast`），要改用 `AppendLater`；内核专用记账碰上另一笔广播时会排队等待；`ModelHistory()` 给 loop 算出模型视角历史 |
| `session/plugin.go` | `Plugin`：领取 `"journal"`，创建并登记 `"sessions"`；`Get(app)` 统一取 Store |
| `persistence/jsonl/{journal,plugin}.go` | `jsonl.Plugin{Root}`：登记唯一的 `"journal"` 能力；Journal 自动建目录和账本文件，一行一条 JSON，关键账 fsync，半行失败回退，重启只修最后半行 |
| `session/history.go` | 历史缓存：有效名单（被 Replaces 收编的旧笔不再算数）+ 增量重算（只有收编发生才全量 rebuild）；chunk 攒缓冲、`assistant/final` 到达时收编 flush |
| `session/store.go` | `Store`：`Create(sessionID, agentID, revision, seed...)` 开新账；`Open` 从 Journal 读旧账；`Release` 写穿并忘掉内存态，使会话可再次打开；v1 旧账明确拒绝 |

完整组装顺序是 `profilejson.Plugin` → `jsonl.Plugin` → `session.Plugin` → `llm.Plugin` → `deepseek.Plugin` → `tools.Plugin` → `localenv.Plugin` → `toolplugins/files.Plugin` → `agents.Plugin` → `loop.Plugin` → `terminal.Plugin`。未来用 `sqlite.Plugin` 替换 `jsonl.Plugin`；两者不能同时装，session 不能省略。

### 4.4 llm/（插座排）

| 文件 | 职责 |
|---|---|
| `llm/types.go` | `Adapter` 接口（`Name()` + `Stream(ctx, req, onDelta) (Reply, error)` 单方法）；`Request{Provider, Model, Thinking, Messages, Tools, MaxTokens, Temperature}`；`Reply{Text, Thinking, Calls, Usage, StopReason}`，StopReason ∈ stop/tool_calls/length/cancelled |
| `llm/errors.go` | 七种 `Err*` 常量 + `Error{Provider, Kind, Message}` + `NewError` + `wrapError`（已统一的错误放行，否则包成 unknown） |
| `llm/service.go` | `Service`：`Register`（重名拒）/ `Complete`（`Stream(..., nil)` 便捷入口）/ `Stream`（按 Request.Provider 路由） |
| `llm/plugin.go` | `Plugin`（注册 `"llm"`）+ `Get(app)` |

`llmplugins/deepseek/` 已是 M6 的真适配器：官方 OpenAI Go SDK 负责调用，插件负责 DeepSeek 的翻译与思考内容处理；测试里的剧本适配器继续只服务单测。

### 4.5 tools/（九步流水线）

| 文件 | 职责 |
|---|---|
| `tools/types.go` | 四条链名：`PreExecute="tools/pre-execute"`、`Execute="tools/execute"`、`Guard="tools/guard"`、`PostExecute="tools/post-execute"`；`Tool{Schema, Execute}`；`Call{ID, Name, Argument, Agent}`；`Result{Output, Status}`；`ResultUnknown="unknown"`；`Decision{Kind, Reason}`（allow/deny/ask）；`Approver` 接口（挂 `"approval"`）；`Outcome{Output, Err}` |
| `tools/registry.go` | 全局工具代码 + Agent 小表；`SetAllowed` 按档案工具名建立白名单，`DropAgent` 只在该 Agent 最后一段会话关闭后执行 |
| `tools/pipeline.go` | **九步，顺序冻结**：① `RecordToolCall`（记不上就不跑）② 前置链（allow/deny/ask）③ 问人（ask → `Resolve[Approver]`，没人挂 = 拒）④ 守卫链（只能拒，返回拒因字符串）⑤ `RecordToolStart`（执行边界，只记一次）⑥ 环绕链 + 本体（重试插件可重跑本体不重走流水线）⑦ 后置链可改结果 ⑧ panic 兜底成 failed（进程不倒）⑨ 终局入账。**唯一不记终局的出口 = 已开跑被取消**：返回 `ResultUnknown`，账上留 start 无 result，交给恢复流程。`runScopedChain` 递归折叠让链跨作用域聚合（父层在外） |
| `tools/recover.go` | `RecoverDangling(book, callID, status, note)`：恢复专用补账窄口，外部不许直接碰 session 记 tool/result——保持"每种事件只归一家记" |
| `tools/plugin.go` | `Plugin`（注册 `"tools"`）+ `Get` |

### 4.6.1 toolplugins/files/（第一个具体工具插件）

`toolplugins/files.Plugin` 领取 `"files"` 和 `"tools"`，把 `write_file(path, content)` 登记进工具菜单。它只会通过 `files.Store` 写工作目录下的相对路径，不直接使用 `os`；以后同类的 `read_file` 也放在这个目录。

### 4.7 agents/（小红管理处）

| 文件 | 职责 |
|---|---|
| `agents/profile.go` | `AgentProfile`（id、版本、模型、人设、工具名、归档）和 `ProfileStore`；档案由持久化插件提供 |
| `agents/service.go` | `Service` 是对外入口：管理档案和会话；`ListAgents` / `ListSessions` 给 UI 做菜单；`Runner` 是 loop 必须实现的会话搬运合同；同一 Agent 可并行多会话，恢复读账本封面的 AgentID 并用当前档案 |
| `agents/plugin.go` | `Plugin`：Resolve `"agent-profiles"`/`"sessions"`/`"tools"` → 注册 `"agents"` + `OnCleanup(CloseAll)` |

### 4.8 loop/（搬运工，Actor 形状：私有状态 + 收件箱 + 单 goroutine 串行）

| 文件 | 职责 |
|---|---|
| `loop/inbox.go` | 三条投递通道：`TargetNextTurn`（开新轮队列）/ `TargetNextStep`（中途话队列）/ `TargetMemo="context"`（小抄盒，不响铃不占待办）；**投递先 `RecordDeliver` 落账再入队**（崩溃不丢）；`takeNextTurn`/`takeNextStep`/`takeMemos`；`restore`（恢复时把未领投递放回队）；`pending` 只查前两条（小抄不算活） |
| `loop/agent.go` | `Conversation` 门面：一段会话的 `SubmitFollowup`/`Steer`/`InjectMemo`/`WaitIdle`/`Cancel`/`State`/`Book`/`Close`；`Close` 会取消并等 Driver 退出，避免会话已释放还在后台跑；同时暴露 `AgentID` 与 `SessionID` |
| `loop/runner.go` | 实现 `agents.Runner`：接收 agents 准备好的会话材料，恢复旧账后交还尚未启动的 Conversation |
| `loop/driver.go` | 主循环：领消息 → `runTurn`（claim 变 user 消息 → turn/start）→ `runStep` 步循环：`step/start` → 领中途话和小抄 → 组装 `llm.Request`（系统提示 + 小抄 system + 账本投影历史）→ `RecordSnapshot`（存当年真发的请求 JSON）→ **`Flush`（问模型前的危险边界）** → `Stream` 边吐边 `RecordChunk` → `RecordAssistantFinal`（收编 chunk，seq 列表当 Replaces）→ 没工具调用就收步，有就逐个过九步流水线 → 直到没活。取消善后：已吐字固化 interrupted 定稿；`stepOpen` defer 兜底补 `step/end`。`Checkpoint="loop/pre-step"` 是预留位还没用 |
| `loop/recover.go` | `recoverFromLedger`：扫旧账一遍——未领投递回队并唤醒；有 start 无 result 的调用经 `tools.RecoverDangling` 补 unknown（正文：操作断在半路、结果不明、你自己判断要不要重做、拿不准先问我）；无 start 的补 skipped（断在开跑前，可以直接重试）；悬空 chunk 固化成 interrupted 定稿；缺的 step/end、turn/end 补上 |
| `loop/plugin.go` | `Plugin`：Resolve `"agents"`/`"llm"`/`"tools"` → 建 Runner → 向 agents 登记 |

### 4.8.1 ui + uiplugins/terminal（终端前端）

`ui` 只定义 `Service.Run(ctx)` 这个启动口，避免为了“未来 Web”提前造通用 UI 大接口。`uiplugins/terminal.Plugin` 用 Bubble Tea 画 TUI，领取 `agents`、`llm`、`tools`，登记 `"ui"`；运行后用侧栏建/选 Agent、开/恢复会话，正常消息交给 `Conversation.SubmitFollowup`。历史直接从 `Conversation.Book().Events()` 读，实时过程订阅 `session/appended` 来显示逐字、工具调用和工具结果；订阅回调只把事件塞进自己的内存队列，慢终端不会卡住同步记账。`main` 是唯一直接领取 `ui` 的地方。

### 4.9 workspace + localenv（工作空间能力 + 本地插件）

| 文件 | 职责 |
|---|---|
| `workspace/files/store.go` | 只放 `Store{Read, Write, List}` 接口、`Entry` 和 `Get(app)`；不碰本地磁盘 |
| `workspace/process/types.go` | 只放 `Spawner.Spawn/Close`、`Handle`、`Command`、`Exit` 和 `Get(app)`；不启本地进程 |
| `workspace/shell/runner.go` | `Runner.Run`：用 Spawner 起进程、立即关 stdin、同时排空 stdout/stderr、等退出；两路输出各自截断，不切出半个 UTF-8 字符 |
| `localenv/files.go` | 本地 `files.Store`：只接受根目录下的相对路径，写时创建父目录 |
| `localenv/process.go` | 本地 `process.Spawner`：每个进程单独建进程组，ctx 取消或 Spawner/App 关闭时杀整组；`Wait` 可重复读同一结果 |
| `localenv/plugin.go` | `Plugin{Root, OutputLimit}` 用同一个 Root 造出本地 files/process，再成对登记 `"files"` / `"process"` / `"shell"`；本地构造函数不对外公开 |

当前是为看清心智主干做的 macOS/Linux 本地直连版，根目录不是沙箱。

### 4.10 测试文件

M1-M4 的原有测试不变。M5 新增 `localenv/{files,process,plugin}_test.go` 和 `workspace/shell/runner_test.go`，全部用真文件和真进程。`workspace/files` / `workspace/process` 只放接口，行为测试跟着实现放在 `localenv`。

---

## 5. 当前状态

### 5.1 已完成

- **M1 core**：App + 四种分发 + 作用域（commit `3ae38a8`、`e026373`）
- **M2 session**：账本 + chat 词汇（含 dsh 对照审查后的四项修正 + 黑话清理，commit `d966e78`、`30eae7c`）
- **M3 llm + tools**：插座排 + 九步流水线（commit `163e71c`、`c56455c`）
- **M4 loop**：收件箱两种账、Driver 主循环、取消固化、崩溃恢复（含路线 2 裁决，commit `c91ec07`、`831ad23`）

`go test -race ./...` 与 `go vet ./...` 全绿。`files` / `process` 是纯接口包，没有单独行为测试；唯一直接第三方依赖是官方 `openai-go/v3`，供 DeepSeek 适配器使用。

### 5.2 M5 完成内容

- M5 新增 `files.Store`、`process.Spawner/Handle`、`shell.Runner` 三个小接口及本地实现。
- `localenv.Plugin` 用同一个根目录成对登记 `"files"` + `"process"` + `"shell"`；App 关闭时杀整个进程组。
- 16 个新测试覆盖真文件共享、持续回话、信号、取消、超时、截断、关 stdin、子进程和 App 收摊。
- M5 当前是 macOS/Linux 本地直连版，根目录不是安全沙箱；没有 PTY、进程池、远程执行和资源配额。

### 5.3 M6 已完成的三小块：JSONL 账本 + Agent 档案 + 打开旧会话

- `persistence/jsonl.Plugin{Root}` 是真正的 App 插件：先登记 `"journal"`，再由 `session.Plugin` 同步领取。
- 它不订阅广播记账；session 直接同步调用 Journal，所以写盘失败就是记账失败。
- 流式 chunk 可暂存到 `Flush`；关键账立即 fsync；写到一半会截回原位置；重启只截断没有换行的最后半行。
- `persistence/profilejson.Plugin{Root}` 登记 `"agent-profiles"`；Agent 档案保存模型、人设、工具名、版本和归档状态。
- `session.Store.Open(id)` 只读回指定 id 的整本账并重建内存；`agents.Service.ResumeSession(id)` 从封面找到 Agent 后交给 loop 找回待办、补可证明缺失的收尾。
- 已有 JSONL / session / loop 的真实重启测试；SQLite 将来做同级插件替换 JSONL。

### 5.4 M6 已完成：用户目录、DeepSeek SDK、YAML 组装和终端 UI

- `compose.Open(home, workspace)` 只把 `workspace` 交给所选执行环境；用户私有目录是 `~/.harness`，其中 `config.yaml`、`agents/`、`sessions/` 均按 Unix 私有权限创建。
- 首次运行会写没有密钥的 YAML 模板并清楚退出。`plugins` 选择档案存储、账本、模型适配器、执行环境、工具插件、Runner 和 UI；四个固定插座自动安装。
- 配置名通过普通 `switch` 对应到已经编进二进制的插件，安装顺序仍由代码固定；未知字段、未知名称和重复名称会在启动前报错，不支持配置叠加、动态加载或热更新。
- `llmplugins/deepseek.Plugin` 使用官方 `github.com/openai/openai-go/v3`，显式 API key / BaseURL；流式可见文字、工具分片、用量和 DeepSeek `reasoning_content` 都会归一化。
- `AgentProfile`、`loop.AgentConfig`、`llm.Request` 现在都有 Provider、Model、Thinking。旧档案可读但不可开始或恢复，会提示更新；不再猜默认服务商。
- `assistant/final` 和 `chat.Message` 保存 Thinking。历史会把一条最终回复的文字、Thinking 和其后 tool/call 合成同一条 assistant，再放工具结果，确保 DeepSeek 下一步能恢复 reasoning_content。
- `uiplugins/terminal` 是当前唯一 UI：Bubble Tea TUI 提供侧栏、会话历史、输入框和帮助页；支持建 Agent、选/新建/恢复会话、发消息、`:history` / `:cancel` / `:exit`；它从账本事件显示真实过程，不直接调用 loop。
- `go run ./cmd/edith-harness --dump-config` 只读取并校验 `~/.harness/config.yaml`，把标准 YAML 输出到 stdout 后退出；输出可再次作为配置导入，因而会包含原配置中的密钥，禁止公开粘贴。

### 5.5 已完成验证

- 全部默认测试离线；DeepSeek 测试用 `httptest` 检查 SDK 请求 JSON、SSE 思考/文字/工具碎片/用量和错误归类。
- YAML 模板、坏配置、插件选择、固定安装顺序、权限、显式 provider 路由、旧档案修复、Thinking 历史重放和两步工具续聊都有测试。
- 终端 UI 有一条真实链路测试：终端输入 → `agents` → 真 loop → 账本事件 → 终端显示；另有历史显示测试，确保流式碎片不会和定稿重复、Thinking 不会泄露到屏幕。
- `loop/crash_recovery_test.go` 用真实 JSONL 和子进程 `SIGKILL` 测五种恢复状态：未领消息继续办、模型请求窗口不重发、未开跑工具补 skipped、已开跑工具补 unknown、尾行撕裂只删半行。`persistence/jsonl/journal_test.go` 另有短写回滚故障注入。
- `compose.DumpConfig` 有 roundtrip 测试：导出后重新读入，插件安装顺序保持一致。

### 5.6 后续可做：真实 API 烟测与小收口

读 `docs/计划书/M6-组装冒烟计划.md` + `失败边界与恢复状态机.md`。要点：

- `cmd/edith-harness/main.go` 已接真实终端 UI；`--dump-config` 只做配置导出，不组装 App、不启动 UI。
- **真文件版 Journal（JSONL）**：一行一条 JSON；写前记偏移、失败截回；fsync；烂尾修复只扔"可证明没写完"的最后一行，中段损坏拒读整本。
- 一个真模型 adapter（DeepSeek 或 OpenAI 兼容）走真实 API；一个有副作用的烟测工具（写文件）。
- `toolplugins/files.Plugin` 已实现第一个具体工具：`write_file` 领取 files，再登记给 tools；待带真实密钥跑一遍完整闭环。
- 重启恢复红队升级：每个提交点前后强杀 + 故障注入（短写/坏尾）；恢复后待办找回、坏尾只修未提交那条、start 无 result 进待裁决不自动重跑。
- dsh 源码里两个可用技巧（参考 `reference/deepseek-harness` 的 persistence 层）：chunk 多笔打包存一行（省 56-60% 空间）；link+unlink 原子发布新文件。
- 七条闭环全过 = **内核完工，进入插件史**。

---

## 6. 与 dsh 参考源码的关系

`reference/deepseek-harness/` 是 DeepSeek 开源的 TypeScript 原版。**只许看、不许抄**（`reference/说明.md` 原话：不可直接使用）。我们只借了“YAML 按名字选插件”：原版还有 npm 插件安装、配置层叠、热更新和 agent 运行时自写插件；我们是静态编译，YAML 只能选择已编进程序的大零件，顺序由 Go 代码固定。

---

## 7. 与 Lucy 的协作规矩（重要，违者她真的会生气）

1. **大白话**。工程黑话一律翻译：不说"屏障"说"把攒的账立刻写完的按钮"；不说"投影"说"从账本算出来的消息列表"；不说"负载"说"广播里带的东西"。她看不懂 = 你没说清。
2. **问什么答什么**，一句话答完，不主动扩展。她要细节会追问。
3. **代码风格红线**：err 两行写法（见 §3.2）她盯得最紧。
4. **不自动提交**。写完停下等 review，她说"提交"才 commit。
5. 她喜欢在动手前听到"一句话说明白这节做什么"；喜欢 ASCII 图和 Mermaid 极简图。
6. 讲解时用比喻（账本/搬运工/长工/小时工/插座排/门铃），这些是项目里的官方比喻，可以复用。
7. 她的英文一般，所有对话和注释用中文（commit 正文英文是唯一例外，那是钦定格式）。

---

## 8. 陷阱清单（我们踩过的坑，你别再踩）

| 坑 | 现象 | 规矩 |
|---|---|---|
| 广播回调里记插件自定义事件 | 直接 `AppendEvent` 会被 `ErrInBroadcast` 拒绝；曾因递归排水爆栈 | 改用 `AppendLater`；别把 `appendLocked`/`broadcastAndDrain` 的拆分合回去 |
| Go 方法不能带类型参数 | `func (a *App) foo[T]()` 编译不过 | 用包级泛型函数（`core.Intercept`、`tools.runScopedChain` 都因此是包级） |
| 空结构体指针全指向同一地址 | 测试里 `&fakeRegistry{}` 指针比较失真 | 假对象加个 tag 字段 |
| WaitIdle 早退 | 搬运工刚把队列领空、还没正式开轮时，外面误以为已经干完 | 除了 busy 和队列，还要看搬运工是否已经接活（`working`） |
| 按字节切汉字 | `Output[:100]` 切出烂字节，JSON 编码变 � 还多两字节 | 用 rune 切 |
| 同一 App 重复 RegisterService | 直接 panic（设计如此） | 第二个场景用子作用域遮蔽 |
| commit message 里的引号断 shell | `"outcome unknown"` 掐断命令 | 用 heredoc：`git commit -F -` |
| `docs/主要心智模型 ` 目录名尾随空格 | 路径写错找不到文件 | 复制路径时带上空格 |
| RecordDeliver 带 Target 参数 | 改签名后旧测试编译失败 | 改公开记账方法签名要全项目 grep 一遍调用点 |

---

## 9. 悬而未决 / 小事记

- `AGENTS.md` 施工依据里的文档路径是搬家前的旧路径，待更新（见 §1）。
- `loop.Checkpoint = "loop/pre-step"` 是预留常量，还没人消费——留给将来的检查点插件。
- std 工具集（bash/文件/网络/技能/待办）**整体挪到内核完工之后**（M6 计划书终审修正），M5/M6 期间不做。
- SQLite 版 Journal 是将来的插件，M6 只做 JSONL。
- 事件格式的迁移函数表（旧版本事件 → 新版本）在设计构想里提了，FormatVersion=1 期间用不上，真要 bump 版本时再做。

---

## 10. 给接手 AI 的开工建议

1. 读 `AGENTS.md` → 本文 → `docs/计划书/Go框架设计构想.md`；
2. `go test -race ./...` 跑一遍确认起点是绿的（应该是 127 个测试全过）；
3. 读 `docs/计划书/M6-组装冒烟计划.md` 和根目录恢复状态机；
4. 给用户一句 M6 要做什么的说明，等她说开工。

祝顺利。这个项目的好玩之处在于：规矩定得极死，死到后面全是自由。
