# 交接文档 —— edith-harness

> 最后更新：2026-08-25。当前内核已完成 Project → Session → Agent 模式重构；TUI 暂停，下一阶段做 Web UI。

## 一句话认识项目

edith-harness 是一个 Go 写的静态插件化 Agent 框架：`core.App` 保管能力，插件把能力登记进去或领取别人的能力，账本是对话和恢复的唯一事实。

## 当前核心模型

```text
Project：一个工作目录
  └─ Session：一段独立对话
       ├─ 锁定一个 Preset 版本
       └─ Runtime Agent：真正运行对话，ID = Session ID
```

- Project 管工作目录和它的会话。
- Preset 是可复用的 Agent 模式：Provider、Model、Thinking、人设和工具白名单。
- Preset 修改时只追加新版本。旧 Session 恢复时仍用创建时的版本。
- agents 不存档案，只负责组装、恢复和关闭运行中的会话。
- loop 是可替换 Runner，负责模型 → 工具 → 模型的主循环。

## 能力表

`core.App` 内部的能力表本质是 `map[string]any`。字符串是抽屉名，值是实际服务。插件之间最终都是通过 Go 接口方法合作。

| 能力名 | 类型 | 用途 |
|---|---|---|
| `project-store` | `projects.Store` | 保存项目 |
| `preset-store` | `presets.Store` | 保存不可变模式版本 |
| `journal` | `session.Journal` | 保存会话事件 |
| `sessions` | `*session.Store` | 创建和打开账本 |
| `llm` | `*llm.Service` | 模型适配器插座 |
| `tools` | `*tools.Registry` | 工具登记和执行流程 |
| `environment` | `environment.Provider` | 按 ProjectRoot 挂载会话环境 |
| `projects` | `projects.Service` | 项目管理入口 |
| `presets` | `presets.Service` | Agent 模式管理入口 |
| `agents` | `agents.Service` | 会话运行入口 |
| `ui` | `ui.Service` | 界面启动入口 |

`files`、`process`、`shell` 不再是全局能力。每次启动 Session 时，`environment.Provider` 会把一套新实例挂到该 Session 的子 App，因此多项目并行不会串目录。

## 代码地图

```text
core/                       能力表、事件、作用域、插件生命周期
chat/                       公共数据词汇
projects/                   项目插件
presets/                    Agent 模式插件
session/                    会话账本
llm/ + llmplugins/          模型插座与具体适配器
tools/ + toolplugins/       工具插座与具体工具
environment/ + localenv/    会话执行环境合同与本地实现
agents/                     运行时会话组装
loop/                       对话 Runner
persistence/jsonl/          账本存储
persistence/projectjson/    项目存储
persistence/presetjson/     模式版本存储
compose/                     读 YAML 并固定顺序装机
uiplugins/terminal/          暂停提示入口
reference/terminal-v1/      已归档的 Bubble Tea 原型，不编译
cmd/edith-harness/           程序入口
```

## Session v3 封面

```go
type Header struct {
    FormatVersion  int
    ID             string
    Title          string
    CreatedAt      time.Time
    ProjectID      string
    ProjectRoot    string
    PresetID       string
    PresetRevision int
}
```

ID 是自动生成的内部身份，Title 可重复。会话封面同时是它恢复时的组装说明书。

## 启动和组装

`compose.Open(home)` 读 `~/.harness/config.yaml`。当前格式是 version 2：

```yaml
version: 2
plugins:
  project_store: projectjson
  preset_store: presetjson
  journal: jsonl
  llm_adapters: [deepseek]
  environment: localenv
  tool_plugins: [files]
  runner: loop
  ui: terminal
```

安装顺序固定：

```text
项目存储 → 模式存储 → 账本 → session
→ llm / adapters → tools → environment / tool plugins
→ projects → presets → agents → runner → UI
```

YAML 只能选择已编进程序的大零件；不做动态插件、热更新或自动依赖排序。

## 账本和恢复红线

- 事件只追加，历史、模型上下文和 UI 都从账本计算。
- 危险操作前必须 Flush。
- 工具未开跑就取消：`skipped`。
- 工具已开跑却没有结果：恢复为 `unknown`，禁止自动重跑。
- 记账走同步 Journal；事件广播只通知观察者，永不落盘。

## 已锁定的取舍

- 静态编译，不做动态插件、AI 自改、热重载或通用 RPC 网关。
- 旧 `agents/` 档案与 v2 Session 不迁移，开发期直接干净升级。
- ProjectRoot 本轮不支持修改；目录移动视为新项目。
- 归档 Project 不删工作目录或历史账本。
- 新会话草稿属于未来 Web UI；首次发送时才调 `agents.StartSession`。

## 下一阶段

做 Web UI（templ + HTMX）：

1. 左侧是 Project → Session 两层导航。
2. 新会话草稿可选 Preset，首次发送才建账。
3. 聊天历史从 `Conversation.Book().Events()` 读，实时过程听 `session/appended`。
4. 首次发送成功后调 `projects.RememberPreset`。

架构图在 `docs/AGent对接/harness-live-map.html`，每次增加真实模块后都要同步维护。

## 验证与提交

改完必须跑：

```bash
go test -race ./...
go vet ./...
git diff --check
```

不要自动 commit。Lucy 明确说“提交”后才提交，commit 说明使用中英双语。
