# 上下文引用 `@`

状态：前沿探索，尚未进入施工。

## 目标

让用户把工作区对象明确交给 Agent，而不是只在消息里手写路径。

## 首版

- `@文件`：保存项目相对路径。
- `@目录`：保存项目相对路径。
- `@代码选区`：保存路径、行号和当时的内容快照。

## 边界

- 使用类型化 `ContextReference`，不从消息文字反向解析。
- 文件和目录不自动塞入全部内容；Agent 按需读取。
- Product 校验路径，Session 保存本次引用。
- 后续可扩展终端输出、Run Diff 和 Subagent 结果。
