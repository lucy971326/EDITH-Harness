# 失败边界与恢复状态机（红队终审 Day-1 交付物）

> 任何进程中断点，都必须能映射到一个明确的恢复状态——不靠内存布尔值猜。
> 本表是 M2/M3/M4/M6 恢复逻辑的唯一依据。

## 状态表（按账本最后一眼能证明什么）

| 重启时账本显示 | 恢复状态 | 恢复动作 |
|---|---|---|
| 轮已正常关闭（turn/end 在账上）| 干净 | 照常等新输入 |
| turn/start 后无任何 user/message | 空轮尝试过 | 记空轮关闭，正常等输入 |
| user/message 有、assistant 无、无 tool/call | 请求未发出 | **不自动重发**（模型可能已收到但我们没记账），标记待裁决 |
| tool/call 有、tool/start 无 | 工具从未开跑 | 落 `tool/result{status:skipped}`（经 tools 窄写入口），轮可继续 |
| 入队在账、领出不在账 | 投递未被消费 | 恢复时重新领出（不丢不重，领出才变 user/message） |
| tool/start 有、tool/result 无 | **副作用窗口** | **自动补"结果不明"**（已裁决）：loop 经 tools 补记 tool/result{status:"unknown"，正文告诉模型"操作断在半路，结果不明，你自己决定要不要重做"}，补完即可继续；禁止自动重跑本体 |
| tool/result 有、step/end 无 | 工具完成但轮没收尾 | 补记收尾，评估是否继续下一步 |
| 尾部是撕裂/残缺记录 | 写入中断 | 只修可证明未提交的最后一条（截断），中段损坏拒绝整本 |
| chunk 后缀缺失、无 assistant/message | 流式中断 | 已收前缀即事实，记 interrupted 消息收尾 |

## 两条总原则

1. **宁可说不知道，不可自动赌**：凡"可能已产生外部副作用"的状态，绝不自动重跑——自动补一条"结果不明"的账交给模型裁决；邮件可能已发出，赌错就是双发。
2. **恢复只信账本**：任何内存状态（开没开跑、发没发出）重启后都是谣言；账本上没有的就是没发生不了证明的，一律进保守态。

## 配套事件词汇（散见 M2/M3/M4，此处汇总）

`tool/start`（执行开始边界，tools 独占）｜"结果不明"补账（status:"unknown"，正文写给模型的决策指引；恢复时 loop 自动补，tools 落账）｜投递/领出各一条（领出才进模型）｜其余按各里程碑计划书。
