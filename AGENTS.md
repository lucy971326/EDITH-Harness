# 开发约束

## 阅读入口

先读本篇，再按任务读相关模块 README 与源码。

| 文档 | 唯一职责 |
| --- | --- |
| [README](README.md) | 项目介绍与启动 |
| [STATUS](STATUS.md) | 当前能力、限制、最近验证 |
| [设计书](docs/设计书.md) | 架构与重要决策 |
| [DATA_MODEL](DATA_MODEL.md) | 数据归属、持久化与恢复 |
| [WEB_UI](WEB_UI.md) | 前端状态、交互与视觉约束 |

完成事实只写 STATUS；稳定决策写对应规范；模块 README 只做源码导读。计划只保留正在执行的内容，完成后吸收独有决策并删除。外部研究在 `docs/codex-docs/`，不是 Harness 规范，不作为默认必读。

## 架构边界

```text
cmd/harness → 显式构造、登记、启动、逆序关闭
React → appserver → conversations → Runner → Loop → Tools
               └→ 各领域公共服务
```

- 单一 Go module；后台按领域放 `internal`，具体实现归领域子包，入口接线。定义契约的包不得反向 import 实现；执行与数据领域不得 import appserver 或 Client。`appserver/internal` 仅供接入层使用。
- 不恢复 Host、Product、万能服务容器、旧 import 转发或旧数据兼容。保留有真实填充者的登记处；不为假想扩展预建接口。
- appserver 只管协议、校验、分发与连接；业务判断归 conversations 或所属领域。Client 只保存投影与临时界面状态。
- Go / TS 契约手写，修改时对照方法名、字段、可选性与返回值；TS 检查不证明两端一致。服务端仍校验输入输出。
- 进程、会话、本轮、步骤各自拥有状态。低成本可推导的值不重复保存；不为减少字段合并无关锁、隐藏状态或削减功能。
- 谁创建长期资源，谁负责取消、关闭、等待及初始化失败清理。取得资源立即安排收尾；某项关闭失败仍继续其余清理并汇总错误。appserver 最后启动、最先关闭。

## 运行铁律

- 同会话写请求有序，不同会话并行；Stop 独立取消，不排在 Steer 后，不进入账本或输入队列。
- 同一 Session 只有一个活 Run，唯一归属 Runner.live；准备期也可停止，完整收尾后才放行下一轮。连接断开不停止已接受的 Run。
- 每个已发出的 tool call 恰好配对一个 tool result；取消也要补齐，停止后不启动剩余工具。
- 一次模型输出及整批工具结果不可插入；检查点取尽当时的 Steer 与协作回报，按到达顺序落账。尚未落账的 Steer 在停止时拒绝，协作回报由来源保留重试。
- 耐久事实先保存再发布；Snapshot 与订阅无空档，慢连接不阻塞 Runner。重启标记未完成轮次中断，不自动续跑。
- Session 只记对话；审批、运行结果、进程、UI 状态各归所属模块。用户数据经 persist 可靠读写，领域自己负责格式、校验和恢复。

## 代码风格

- 可读性优先；标识符英文、注释中文、commit 双语。不创建 `manager.go`、`utils.go`、`common.go`。
- 一个领域默认同包分文件；只有独立职责、稳定边界和实际替换需求才拆子包，不为缩短函数建立 helper 链。
- 每包一个主要公开构造入口，只校验依赖与组装。接口处理用依赖结构体与具名方法，避免捕获服务的匿名 Handler；方法登记留在 appserver，main 只组装。
- 数据与契约放定义者 `types.go`；活对象与方法同文件；包内类型放使用处。导出类型注释首句标明“数据。/ 契约。/ 活对象。”。
- 结构体字段与接口方法按职责用空行和简短中文注释分组；锁说明保护范围。字段能少则少，不能硬少。
- 流程按执行顺序展开，错误就地返回，循环用 continue 跳过无关项；允许函数稍长。检查与修改保持原子性，锁、错误优先级和 defer 收尾直接可见。
- `err := f()` 与 `if err != nil` 分行。普通包返回错误；仅进程边界或无法返回的后台入口打印一次。领域定义业务错误，接口边界统一翻译，不重复执行业务校验。
- 命名一致、避免难辨缩写；注释说明原因与边界，不逐行翻译。协程启动、取消和等待应能在同一处看清。
- 泛型只用于必要的公共类型转换，参数名用 `Input / Output`。请求的校验、解码、调用、编码和输出校验保持主线可见。

## 工作与验证

- 改动前读相关代码、调用链和约束，三句内说明目标、主干与验证。区分事实、推测与未知；不覆盖无关改动，不顺手扩大范围。
- 发现冗余、设计拿不准或拟新增抽象时，先按“相关文档 → CodeGraph → reference/codex 源码”调研；索引不可用就直接读源码。只借鉴当前需要的部分，不能把参考当作免验证答案。
- Bug 尽量先复现或用最小失败测试定位根因。测试只保护核心真实回归；复用已有文件与表驱动用例，不逐层重复测试薄转发或展示细节。
- 新增测试文件或明显扩大测试量前，说明防止的具体故障。默认不新增 UI 渲染、布局、快照或模拟交互测试。
- 开发中只跑必要的小检查；代码完成后统一执行一次 `make agent-check`，发布前用 `make test` 串行验收。后续未影响已检查范围时不重跑；纯文档改动检查事实、链接与 diff 即可。
- 搜索优先 rg。命令只为当前交付取证，不反复执行同义检查；用户要求不执行命令后本轮立即停止命令。
- 完成时核对原始目标、diff、结果与未验证项；长任务记录约束、进度、未决问题，达到目标后停止。
- 默认简短中文，复杂概念用极简 ASCII。用户有重度 ADHD，避免信息堆砌。
- UI 由用户运行 `make run` 并截图验收；Agent 不操作浏览器做视觉验收，除非用户明确另行要求。
