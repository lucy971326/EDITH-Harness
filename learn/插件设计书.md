# 插件设计书

开工按这篇。话术（登记处 / 插槽 / 活对象、A / B）见 `项目心得.md`。铁律见 `给后来的AI.md`。

参考 DeepSeek Harness 和 pi，**不照抄**。Go 框架。静态编译。无热加载。无 AI 自改。前端：Go + templ + htmx。

---

## 1. 根是宿主，不是聊天

进程是一张服务表。聊天只是表上的一摊。

```
Host（宿主）                        桌子，不是插件
  ├─ HTTP            登记处 B      谁都能占路径
  ├─ 页面洞          登记处 B      布局挖洞，别人填片段
  ├─ Session         整份 A        对话账
  ├─ llm             登记处 B      适配器来填
  ├─ tools           登记处 B      工具来填
  ├─ kinds           登记处 B      LLMAgent / GraphAgent 来填
  ├─ Runner          整份 A        外壳；还没跑完的 Run 也在它手里
  ├─ 狼人杀          整份 A        可以 Get Runner，棋盘不进账本
  └─ 电影播放器      整份 A        零依赖上面那些。自己的路径
```

`RegisterService` / `Resolve`。定义者出契约，提供者装上，消费者 import 定义者。彼此不 import。

没有两份 Agent。Kind 是怎么想；正在跑的是 Runner 还没返回的 `Run`。

---

## 2. 宿主 Host

不是插件，是插件挂上去的地方。类型叫 `Host`，不叫 `App`（`App` 像整程序就是聊天）。

```go
type Host struct {
    services map[string]any
}

func (h *Host) RegisterService(name string, v any) { h.services[name] = v }
func (h *Host) Resolve(name string) (any, error)   { /* 没有就 error */ }
```

插件 `Start` 时往上挂；`Close` 倒序拆。没有这张表，后面谁都没处放。

---

## 3. Session

整份服务（A）。对话账。

```go
type Session interface {
    Append(...)          // 在「现在站的地方」下面记一行
    History() []Message  // 从这里往回走到开头，问模型
    Branch(id string)    // 只挪指针。历史不改
}
```

只记人 / 模型 / 工具结果，加上压缩后的摘要。todo、审批、游戏棋盘，**放插件自己的结构体**。

形状学 pi：可以分叉。不是 DSH 那种只能往下接、什么都往里扔的流水账。

```
用户: 帮我改登录
  └─ 模型: 用方案 A
        ├─ 用户: 就用 A     ← 指针在这
        └─ 用户: 改用 B     ← 旧路还在，没删
```

只有一份。文件是这份的存法。热路径（字一个个出来）不写磁盘。一回合结束 / Runner 返回再写。

不要两份长期分开养（pi：内存一列消息 + 文件一棵树，来回抄）。`History()` 是现算的，不是第二本活数组。

**屏幕不听账本。** Session 是听众（一句说完了 `Append`），不是喇叭。

| 要干什么 | 听谁 |
|---|---|
| 现在这句正在往外蹦 | 这一轮 `Runner.Run` |
| 打开旧对话、画分叉 | 读 Session |

喇叭就这一个。标题、压缩、计数：听同一轮 Run。todo：屏幕在「工具结束」时去读它的服务。别每个插件各喊各的。

---

## 4. llm

登记处（B）。不是入口。

```go
type LLM interface {
    Stream(ctx context.Context, req Request) (<-chan Chunk, error)
}

host.Resolve("llm").Register("deepseek", deepseekAdapter)
```

适配器来 `Register`，`LLMAgent` 要流式输出时再 `Get`。换模型 = 换适配器。不换 Runner，不换 Agent 接口。

GraphAgent 可以完全不碰它。人从不直接调 llm，只调 `Runner.Run`。

---

## 5. tools

登记处（B）。

```go
type Tool interface {
    Name() string
    Call(ctx context.Context, args Args) Result
}

host.Resolve("tools").Register(bash)
host.Resolve("tools").Register(spawn)
```

问模型那一轮，`LLMAgent` 做的是 `Get("tools")`，把名单塞进请求。提示词只拼文本，不代收 schema。

某个 Agent 可以名单更窄，也可以一个工具都没有。异步子任务的 spawn / wait / stop 也只是这里的三个工具，去碰 Runner。

---

## 6. Agent（Kind）

一种程序怎么想。往 `kinds` 上登记，是登记处（B），不是一个活着的 Agent 服务。

```go
type Agent interface {
    Run(ctx context.Context, inv Invocation) error
}

kinds.Register("llm", LLMAgent{})
kinds.Register("graph", GraphAgent{})
```

换范式换这个，不换 Runner。session 在 `Run` 的参数上。同一份 Kind 可以跑很多 session。

`LLMAgent` 会 `Get("llm")`、`Get("tools")`。`GraphAgent` 可以两个都不碰。

用户填的是 Spec（人设、模型名、要用哪些工具）。Spec 不是插件，是数据。不要再做一层 CustomAgent。用户不热加载 Go。Kind 要编译进去。

子任务可以是另一种 Kind：父是 llm，子可以是 graph。那是下一份 Spec，不是 Agent 接口变了。

### Invocation

`Agent.Run` 吃的就是这个。钉死三样：

```go
type Invocation struct {
    Session Session
    Message UserMessage
    Notify  func(Event) // Runner 填：字、工具起止从这里出去
}
```

`Session`：`History()` 给模型，说完了 `Append`。  
`Message`：人这轮新说的。  
`Notify`：不进账本；Runner 拿去给屏幕。没有它，字出不去。

Spec 不在这里。Runner 先 `Build(spec)` 再 `Run`。取消用外面那个 `ctx`。

DeepSeek 把转圈焊进活 Agent。我们不学。换 loop = 换 Kind，不是换 Runner。入口是 `runner.Run`，不是 DSH 的 `Followup`。

---

## 7. Runner

整份服务（A）。一场调用的外壳。一般只装一个。

不管怎么想。找账本、按 Spec 把 Agent 建出来、把话交出去、一句说完再记。屏幕听的是它这一次 `Run`。

还没跑完的那些 `Run` 也在它手里。不是第二份 Agent 插件。

```go
type Runner struct {
    kinds    Kinds
    sessions Sessions
    specs    Specs         // 每个 session 一份 Spec（数据，不是插件）

    mu   sync.Mutex
    live map[string]*run
}

type run struct {
    sessionID string
    cancel    context.CancelFunc
    done      chan error
}

func (r *Runner) Run(ctx context.Context, sessionID string, msg UserMessage) error {
    spec := r.specs.For(sessionID)
    a, err := r.kinds.Build(spec)
    sess := r.sessions.Get(sessionID)
    return a.Run(ctx, Invocation{
        Session: sess,
        Message: msg,
        Notify:  r.forwardToScreen,
    })
}

func (r *Runner) Spawn(sessionID string, msg UserMessage) (string, error)
func (r *Runner) Wait(sessionID string) error
func (r *Runner) Stop(sessionID string) error
```

`Run`：塞进 `live`，卡住直到这轮结束（人说话）。  
`Spawn`：同样开跑，立刻返回 `sessionID`。  
`Wait` / `Stop`：查 `live`，等 `done`，或调 `cancel`。

session 和 Spec 事先就有。Runner 不建账本。

人、HTTP、spawn 工具，进的都是这份服务。

| 层 | 干什么 | 谁换 |
|---|---|---|
| Runner | 一场调用的外壳。不管怎么想 | 很少换 |
| Agent | 这种程序怎么干活 | **换范式换这个实现** |
| llm | 被 `LLMAgent` 去调 | 换适配器 |

---

## 8. 对话怎么跑

```
人发一句话
  runner.Run(sessionID, 话)
       │
       ├─ 正在发生（字蹦出来、工具开始/结束、这轮完了）
       │     → 屏幕听这个（同一进程：回调；浏览器：SSE）
       │
       ▼
  agent.Run(Invocation)
       │
       ▼
  llm.Stream(...)               只有 LLM 类 Agent 才 Get("llm")
```

llm 不是第三入口。

---

## 9. 异步子任务

平台功能。不是某种 Agent 自己会的本事。

不要的：工具里面把子 Agent 跑完再返回。  
要的：工具只下令，马上返回。孩子在宿主里自己跑。

```
宿主 / Runner
  父 Run(session 父)     还在转
  子 Run(session 子)     另开一条

三个工具（挂在 tools 上，Get Runner）：
  spawn  再调一次 Runner.Spawn，立刻给 id
  wait   Runner.Wait
  stop   Runner.Stop
```

分叉处是再进一次 **Runner**。Agent 里不分叉。子任务有自己的 session，按那一份 Spec `Build`，可以是另一种 Kind。

孩子的寿命不能绑在 spawn 这次工具的 `ctx` 上。工具返回了孩子还得在；只有 `stop` 才杀掉。

父账本只记 spawn / wait / stop 的工具结果。子账本是子自己的。屏幕要看子任务：听子那一轮 `Run`。

不抄 DSH 那一套 subagent 插槽（六个后端、jobs、Activation）。v1：进程内 goroutine、三个工具、深度限制是 Runner 的策略。

---

## 10. HTTP

登记处（B）。宿主挖好路径洞，谁都能来占。

```go
type HTTP interface {
    Register(path string, h Handler)
}

http.Register("/chat", chatHandler)     // 里面调 Runner.Run
http.Register("/movies", moviePage)     // 零依赖对话
```

请求 POST；字往外蹦 SSE。都走这份登记处。v1 不用 WebSocket。

同一进程（CLI）：Run 调一下听的人。完了。浏览器过不去进程，才需要管子：

```
点按钮 / 发话       → POST
字蹦出来 / 工具在跑 → SSE（还是 HTTP，只下行）
```

SSE 就是把回调送到页面。DSH：请求 HTTP，通知两条只下行 WebSocket，而且是账本转给浏览器。我们不学后半。

---

## 11. 页面洞

登记处（B）。布局挖洞，别人填 templ 片段。

```
布局 templ
  ├─ 侧栏          整列洞：再占会换掉整列
  ├─ 中间聊天
  ├─ 右边
  ├─ 浮层          列表洞：新 id 往旁边加
  └─ 输入条下面    列表洞：goal 条子这种
```

```go
type Pages interface {
    Register(slot string, fragment templ.Component)
}

pages.Register("conversation.dock", GoalBar)
http.Register("/movies", MoviePage)            // 整页产品，不塞聊天洞
```

和 HTTP 分工：HTTP 是路径，这个是页面上的洞。聊天气泡旁加一块 → 填洞。整页产品 → 自己的 HTTP 路径。

不在浏览器再跑一棵插件树，不抄 React 那套类型链。洞在服务端填好，htmx 换片段。

「喇叭就一个」只约束**对话这一摊**。狼人杀可以有自己的页面更新，别倒进账本。

---

## 12. 狼人杀

整份服务（A）。一款游戏，不是对话功能。

棋盘是它自己的结构体。不进 Session。

```go
http.Register("/werewolf", w.Page)
runner.Spawn(wolfSpec, "你是狼人，今晚刀谁")
```

`Get` Runner（要 AI）。页面听游戏服务，不听某轮聊天的 `Notify`。

验的是：根是宿主。游戏和聊天并列，不是挂在 Runner 下面的插件。

---

## 13. 电影播放器

整份服务（A）。一款产品。

什么对话都不 `Get`。

```go
func Movie(host *Host) {
    http := host.Resolve("http").(HTTP)
    http.Register("/movies", MoviePage)
}
```

不碰 Session、Runner、Agent、llm、tools。只占一条 HTTP 路径。

验的是：宿主上可以有零依赖聊天的东西。

和 DSH 的差：他们用户能热加载一点 TS。我们加一种新服务要编译进去。这是故意的。

---

## 14. 明确不抄

| 不抄 | 学 |
|---|---|
| DSH 账本当产品仓库 + 喇叭 | 账本只记对话 |
| DSH 转圈焊进活 Agent | Runner 在外面 |
| DSH 提示词代收工具 schema | 文本归文本，工具现取 |
| DSH 四十多个可替换面当内核 | 上面这些就够 |
| DSH subagent 六个后端 + jobs | Runner.Spawn / Wait / Stop |
| pi 扩展往同一棵树塞私货 | 插件自己的结构体 |
| pi 内存一列 + 文件一棵，两头抄 | 一份 Session，`History` 现算 |
| pi harness（没启用）三仓状态机 | 太重 |
| 浏览器第二棵 Cordis、双 WebSocket | templ 洞 + POST/SSE |
| 热加载 Go、AI 自改 | 静态编译。Kind vs Spec |
| 「正在跑的 Agent」再挂一份服务 | 就是 Runner.live |

---

## 15. 怎么写代码

一个领域一份服务。服务里面按职责拆文件。**真能换的才做插槽**（B）。别把小能力都 `RegisterService`。

```
Plugin    安装：Start 里挂上、OnCleanup 里拆
Service   给人用的那份对象（A 整份，或 B 那张登记处）
插槽      B 的 Register 口。不是第三种插件
```

能力名是字符串，不改成类型 Key，不用函数把名字藏起来。对不上就是组装错误。

```go
host.RegisterService("runner", r)
r, err := core.Resolve[*runner.Runner](host, "runner")
```

### 包怎么拆文件

默认同一个 Go 包里分章节，不先拆子包。

```
plugin.go          Start：Resolve 依赖、建对象、登记、清理
service.go         对外能做什么（菜单，不放状态、长流程）
types.go           跨包或要落盘的数据
<角色>.go          核心结构体
<角色>_<动作>.go    一条完整流程
```

例子（Runner，不是旧的 Conversation 管家）：

```
runner/
  plugin.go
  service.go          Run / Spawn / Wait / Stop
  types.go
  runner.go           结构体 + live
  runner_run.go       开一轮、塞 live、转 Notify
  runner_spawn.go     立刻返回 id
```

`service.go` 是菜单。同一结构体的方法可以分文件，靠同一个 receiver。

看目录和 `service.go` 就能说清这摊干什么；找一条流程能直接落到那个 `*_动作.go`。

### 命名

| 词 | 干什么 |
|---|---|
| Registry | 登记、查找、重复检查、撤销（kinds / llm / tools / HTTP / 页面洞） |
| Store | 拥有并读写一组数据（Session、Spec） |
| Adapter | 翻译两种协议（llm 适配器） |
| Plugin | 安装和卸载 |

宁可名字长，别让人猜。不要再做一个 `conversationManager`。活着的 `Run` 在 `Runner.live`。

### 什么时候才拆子包

三件都满足才加一层目录：能独立替换；边界稳定；不会绕一圈转发。

```
优先：领域包 → 章节文件 → 函数
```

不因为文件长或「以后也许有用」而拆包。

领域怎么切：`core` 是桌子；`session` 一份账；`llm` / `tools` / `kinds` 各一份登记处；`runner` 一份外壳；`http` / 页面洞各一份登记处。Web 是插件，按页面拆文件，不拆成一串新的服务。

### 装上和拆掉

- B 的 `Register` 返回幂等 `unregister`。
- `Install` 中途失败：已 Start 的倒序拆。
- 谁开长期资源，谁关。
- 一次铺一个领域，测过再下一个。不顺手加功能，不改已经公开的能力名。

### 写法

- 可读性优先，不炫技。
- `err := f()` 和 `if err != nil` 分成两行。
- 每包一个主要公开构造入口；构造只校验依赖和组装。
- 接口只为现在的边界，不为未来预建。
- 不要没有主人的 `utils.go` / `helpers.go` / `common.go`。

### 讲一个插件时四问

一次只讲一个。先说是不是插件，再说已经有了还是计划。

```
【它是什么】     一句话
【提供能力】     别人 Resolve 到什么
【使用能力】     它 Get 哪些已有的
【填充插槽】     填哪个 B；没有就写「不填」
```

不是插件（比如 Spec）：它是什么、干什么、谁产生/谁用。先人话，再代码名。不把没做的说成已经有了。
