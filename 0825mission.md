# 0825 Mission

## Web UI

- 下一阶段做 Web UI：Go `html/template` + HTMX，暂不做原生 GUI。
- `uiweb` 是一个 UI 插件：领取 `agents` 等已有能力，负责网页入口；不直接调用 loop 内部。
- 首版界面是“Project → Session”两层导航，Agent 模式是每段会话的选项，不是主页第一层。
- 新会话先是空草稿；首次发送时才创建 Session 并锁定当时的 Preset 版本。
- 项目记住上次成功使用的模式，但用户发送前可切换。

## 未来可扩展

- 不让每个新功能都修改网页壳子。
- 网页壳子在需要时提供少量明确位置：设置页、聊天卡片、聊天操作、独立页面。
- 功能的网页伙伴插件领取领域能力和 `uiweb`，再向对应位置 `Register...` 自己的内容。
- 先有第二个真实需求，再增加位置；不提前造通用前端插件系统。

## TUI

- TUI 当前暂停推进；旧源码完整移入 `reference/terminal-v1`，不参与编译。
- Web 跑通后，再决定是否保留或移除 TUI。
