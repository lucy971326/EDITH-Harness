# apply_patch

【角色】把模型给出的 Codex 格式补丁转换为本机文件修改。

【提供能力】向 `tools` 登记 `apply_patch`，支持新增、修改和删除文件。

【依赖】通过 `machine.FileSystem` 读取版本、按版本写入或删除并解析路径；不保存文件状态。

【边界】本包负责补丁语法、上下文匹配、换行保留、并发修改保护和已提交变化；Turn Diff、账本与界面属于后续阶段。
