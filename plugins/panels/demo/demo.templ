package demo

templ content() {
	<div class="grid h-full place-items-center p-6 text-center">
		<div>
			<div class="mx-auto mb-3 grid size-10 place-items-center rounded-xl bg-[var(--color-muted)]">▤</div>
			<p class="text-sm font-medium">面板登记已就绪</p>
			<p class="mt-1 text-xs text-[var(--color-subtle)]">这是独立 demo 插件填入的内容。</p>
		</div>
	</div>
}
